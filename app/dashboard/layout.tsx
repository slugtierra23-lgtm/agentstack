export const metadata = {
  title: 'AgentStack — Dashboard',
  description: 'Your AgentStack dashboard.',
}

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
