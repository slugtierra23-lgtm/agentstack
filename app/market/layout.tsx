export const metadata = {
  title: 'AgentStack — Task Market',
  description: 'Browse and post tasks on AgentStack.',
}

export default function MarketLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
